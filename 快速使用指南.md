# 🚀 快速使用指南（中文版）

## 问题修复完成！

我已经参考 insidebar-ai 项目的方式，对按钮注入进行了全面改进：

### ✅ 主要改进

1. **创建了独立的 CSS 文件** - 更专业的样式管理方式
2. **添加了详细的调试日志** - 可以清楚看到每一步执行情况
3. **改进了按钮插入策略** - 4 个备选方案，成功率更高
4. **添加了延迟重试机制** - 适配 ChatGPT 的单页应用特性

## 📝 现在请这样测试

### 第 1 步：重新构建插件

```bash
cd extension
npm run build
```

你应该看到：
```
✅ Static assets copied.
✅ dist/content-script.css  (CSS文件已创建)
✅ dist/content-script.js
```

### 第 2 步：重新加载插件

1. 打开 Chrome 浏览器
2. 访问 `chrome://extensions/`
3. 找到 "Prompt Optimizer"
4. 点击**刷新按钮**（🔄 Reload）

### 第 3 步：打开 ChatGPT 并查看调试信息

1. 访问 https://chatgpt.com
2. 按 **F12** 打开开发者工具
3. 切换到 **Console** 标签
4. 你应该看到这些日志：

```
✅ [Prompt Optimizer] Content script loaded on: https://chatgpt.com/...
✅ [Prompt Optimizer] Content script initialized
✅ [Prompt Optimizer] Document ready, mounting...
✅ [Prompt Optimizer] mount() called
✅ [Prompt Optimizer] ✅ Supported host: chatgpt.com
✅ [Prompt Optimizer] Trying selectors: ["div[role=\"textbox\"]", "#prompt-textarea", ...]
✅ [Prompt Optimizer] ✅ Found input element: div[role="textbox"] <element>
✅ [Prompt Optimizer] Creating button...
✅ [Prompt Optimizer] Trying insertion strategies...
✅ [Prompt Optimizer] Strategy 1 - Plus button found: <button>
✅ [Prompt Optimizer] ✅ Button inserted via Strategy 1 (next to + button)
```

### 第 4 步：查找按钮

在 ChatGPT 输入框的**左侧**，你应该看到一个**黑色的圆角按钮**，上面写着 **"优化指令"**。

位置示意：
```
[+] [优化指令] [___输入框_______________] [🎤] [发送]
     ↑
   这里！
```

## 🐛 如果仍然看不到按钮

### 方法 1：在 Console 中快速检查

复制以下代码，粘贴到 Console 中运行：

```javascript
console.log('=== 快速诊断 ===');
console.log('1. 按钮存在吗？', document.querySelector('.prompt-enhancer-btn'));
console.log('2. 输入框存在吗？', document.querySelector('div[role="textbox"]'));
console.log('3. + 按钮存在吗？', document.querySelector('[data-testid="composer-plus-btn"]'));
console.log('4. CSS 加载了吗？', [...document.styleSheets].find(s => s.href?.includes('content-script.css')));
```

### 方法 2：查看详细日志

看看 Console 中显示了什么：

- ✅ **如果显示 "✅ Button inserted"**：按钮已插入，但可能被隐藏了
- ❌ **如果显示 "❌ Could not find input element"**：输入框选择器需要更新
- ⚠️ **如果没有任何日志**：插件没有加载，需要重新加载插件

### 方法 3：手动创建按钮测试

在 Console 中运行这段代码，手动创建一个测试按钮：

```javascript
const btn = document.createElement('button');
btn.textContent = '优化指令';
btn.style.cssText = `
  display: flex;
  align-items: center;
  justify-content: center;
  height: 36px;
  padding: 0 16px;
  margin-left: 8px;
  border-radius: 9999px;
  border: none;
  background: #000000;
  color: #ffffff;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
`;

// 找到 + 按钮并插入
const plusBtn = document.querySelector('[data-testid="composer-plus-btn"]');
if (plusBtn?.parentElement) {
  plusBtn.parentElement.appendChild(btn);
  console.log('✅ 测试按钮已创建！');
} else {
  console.log('❌ 找不到 + 按钮');
}
```

## 📚 详细文档

如果问题仍未解决，请查看：

1. **`extension/TESTING-QUICK-START.md`** - 详细测试步骤
2. **`extension/DEBUGGING.md`** - 完整的调试指南（8个步骤）
3. **`IMPROVEMENTS.md`** - 所有改进的技术细节

## 💡 反馈问题

如果按钮仍然没有出现，请提供：

1. **Console 的完整日志**（截图或文字）
2. **运行诊断脚本的结果**（上面的快速诊断代码）
3. **Chrome 版本**
4. **ChatGPT 的 URL**（是 chatgpt.com 还是 chat.openai.com？）

## 🎯 预期效果

成功后，你应该能：

1. ✅ 在 ChatGPT 左侧看到 "优化指令" 按钮
2. ✅ 点击按钮优化提示词
3. ✅ 使用 `Ctrl+Shift+O` (Windows) 或 `Cmd+Shift+O` (Mac) 快捷键
4. ✅ 暗黑模式下按钮自动变成白色
5. ✅ 看到详细的 Console 日志（方便调试）

## 📊 改进总结

这次重构参考了 insidebar-ai 的方式：

| 改进项 | 说明 |
|--------|------|
| CSS 文件 | 独立管理，自动适配暗黑模式 |
| 调试日志 | 10+ 条详细日志，清楚标记每一步 |
| 插入策略 | 4 个备选方案，提高成功率 |
| 重试机制 | 100 次重试 + 2秒延迟，适配 SPA |
| 文档 | 2 个调试指南 + 改进说明 |

希望这次能成功！🎉
